version: 1.1

pr_review:
  rules:
    - "Code should be DRY (Dont Repeat Yourself)"
    - "There should no secrets or credentials in the code"
    - "Extremely Complicated Code Needs Comments"
    - "Use Descriptive Variable and Constant Names"
    - "Error Handling Must Be Implemented for services"
    - "Don't log sensitive data"
    - "Follow the Single Responsibility Principle"
    - "Function and Method Naming Should Follow Consistent Patterns"

  auto_review_enabled: false

  # This section allows you to override the repository defaults for specific users
  user_overrides:
    - usernames: ["vontell"] 
      auto_review_enabled: true
      enable_approve_or_reject: false
      auto_summarize_pr: true
